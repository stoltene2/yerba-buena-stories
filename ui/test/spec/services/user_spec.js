'use strict';

describe('user service', function () {

  beforeEach(module('uiApp'));

  //FIXME How do i provide access to the UserService?
  xit('should provide a find function that takes one parameter', function () {
    UserService.find('username');
  });

});
